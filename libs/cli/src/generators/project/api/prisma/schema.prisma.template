
generator client {
  provider = "prisma-client-js"
  output   = "../node_modules/<%- fullProjectName %>-db"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Category {
  id   Int    @id @default(autoincrement())
  name String @unique
}
